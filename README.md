# Pyae Sone (Hardin)

Frontend & Full-Stack Engineer Intern · Bangkok, Thailand 🇹🇭

I am a software engineering intern at the13.ai. I build the admin side of a
multi-tenant commerce platform, from the React screens down to the FastAPI
endpoints and the Postgres tables behind them.

## 🧩 What I Build

- **Operator dashboards**: React and TypeScript screens for shops, messaging
  channels, FAQs and payment settings, each bound to a live REST API.
- **Typed API layers**: Zod schemas that parse every response at the boundary,
  so a shape change on the server surfaces as one clear error instead of
  `undefined` three components deep.
- **Backend slices**: FastAPI routes over PostgreSQL, including file upload to
  S3-compatible object storage with content-type and size checks.

## 🛠 Stack

`TypeScript` · `React` · `Zustand` · `Zod` · `Vite` · `Python` · `FastAPI` ·
`SQLModel` · `PostgreSQL` · `MinIO` · `Keycloak` · `Docker` · `uv`

## 📌 Currently

Building the operator dashboard at [the13.ai](https://the13.ai). Shop, messaging
channel, FAQ and payment settings on the frontend, plus the FastAPI service that
stores and serves payment QR images from object storage.

## 🔗 Connect

- Email: pyaesone99445@gmail.com
